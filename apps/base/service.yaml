apiVersion: v1
kind: Service
metadata:
  name: sigstore-svc
  labels:
    app: sigstore
spec:
  selector:
    app: sigstore
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP 
